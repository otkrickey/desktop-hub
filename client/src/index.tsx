import React from 'react';
import ReactDOM from 'react-dom';

const render = () =>
    ReactDOM.render(
        <React.StrictMode>
            <></>
        </React.StrictMode>,
        document.getElementById('root'),
    );

render();
